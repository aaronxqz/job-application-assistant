"""Job application autofill toolkit.

This package drives a real (visible, persistent) browser session with
Playwright to fill out job application forms from data you provide in
data/profile.yaml. It deliberately does NOT click the final "Submit"
button -- it fills the form and stops so you can review before sending.
"""

__version__ = "0.1.0"
